const defaultTheme = require('tailwindcss/defaultTheme')

module.exports = {
  theme: {
    fontFamily: {
      google: ['Neonderthaw']
    },
    extend: {
      colors: {
        'spotify-green': '#1DB954'
      }
    }
  }
}
